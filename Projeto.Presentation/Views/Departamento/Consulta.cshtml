@model List<Projeto.Data.Entities.Departamento>

@{
    ViewData["Title"] = "Consulta";
    Layout = "~/Views/Templates/Layout.cshtml";
}

<h4>Listagem de Departamentos Cadastrados</h4>
<hr />

<div>
    <strong>@TempData["Mensagem"]</strong>
</div>

<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th>Código</th>
            <th>Nome</th>
            <th>Descrição</th>
            <th>Nº de Funcionários</th>
            <th>Operações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.IdDepartamento</td>
                <td>@item.Nome</td>
                <td>@item.Descricao</td>
                <td>@item.Funcionarios.Count</td>
                <td>
                    <a href="/Departamento/Edicao?id=@item.IdDepartamento" class="btn btn-primary btn-sm">Atualizar</a>
                    <a href="/Departamento/Exclusao?id=@item.IdDepartamento"
                       class="btn btn-danger btn-sm"
                       onclick="return confirm('Deseja excluir esse departamento?');">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <td colspan="5  ">
                Quantidade de cargos: @Model.Count
            </td>
        </tr>
    </tfoot>
</table>

